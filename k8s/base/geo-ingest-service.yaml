apiVersion: v1
kind: Service
metadata:
  name: geo-ingest
  namespace: geoint
spec:
  selector:
    app: geo-ingest
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP